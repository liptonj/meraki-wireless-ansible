---
meraki_api_key: "{{ lookup('env', 'MERAKI_DASHBOARD_API_KEY') | default(vault_meraki_api_key | default(''), true) }}"

meraki_desired_group_policies: []
meraki_group_policy_configs: []

meraki_group_policy_api_retries: "{{ meraki_api_retries | default(3) }}"
meraki_group_policy_api_retry_delay: "{{ meraki_api_retry_delay | default(2) }}"
